<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.accela.constructdemo" >


    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:name=".AppContext"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".HomeActivity"
            android:label="@string/main_view_title"
            android:launchMode="singleInstance" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             Important: This AuthorizationActivity activity must be declared in your Android project's AndroidManifest.xml file because it is required in user authorization.
        				And the value of android:scheme should be assigned to the urlSchema property when AccelaMobile class is initialized.
        -->
        <activity
            android:name="com.accela.mobile.AuthorizationActivity"
            android:launchMode="singleInstance"
            android:windowSoftInputMode="stateHidden" >
            <intent-filter>
                <action android:name="android.intent.action.VIEW" >
                </action>

                <category android:name="android.intent.category.DEFAULT" >
                </category>
                <category android:name="android.intent.category.BROWSABLE" >
                </category>

                <data
                    android:host="authorize"
                    android:scheme="amtest" >
                </data>
            </intent-filter>
        </activity>
        <activity
            android:name=".CitizenTestActivity"
            android:label="@string/citizen_test_title"
            android:launchMode="singleInstance" />
        <activity
            android:name=".AgencyTestActivity"
            android:label="@string/agency_test_title"
            android:launchMode="singleInstance" />
    </application>







</manifest>
